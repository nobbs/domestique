openapi: 3.1.0
info:
  title: Domestique API
  version: 2.0.0
  description: |
    The API contract served by Domestique. JSON property names are camelCase.
    All routes except `GET /healthz` require the browser session cookie;
    state-changing routes also require the same-origin `Origin` header. The
    sign-in routes under `/auth/` are browser navigation rather than API
    operations, and are not described here.
servers:
  - url: /
security:
  - session: []
x-domestique-unmatched-path:
  status: 404
  content:
    application/json:
      schema:
        $ref: '#/components/schemas/Error'
tags:
  - name: browser-api
    description: Programmatic same-origin API used by the embedded UI.
  - name: oauth
  - name: readiness
    description: Loopback readiness listener, served separately from the public handler.
paths:
  /healthz:
    get:
      operationId: getHealth
      security: []
      responses:
        "200":
          description: Process is live.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Health'
  /readyz:
    get:
      tags:
        - readiness
      operationId: getReadiness
      security: []
      x-domestique-listener: readiness
      responses:
        "200":
          description: Local state is ready.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Readiness'
        "503":
          description: Local state is unavailable.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Unready'
  /v1/status:
    get:
      tags:
        - browser-api
      operationId: getStatus
      responses:
        "200":
          description: Current local status.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Status'
        "401":
          $ref: '#/components/responses/Unauthorized'
        "403":
          $ref: '#/components/responses/Forbidden'
        "503":
          $ref: '#/components/responses/Unavailable'
  /v1/tasks:
    get:
      tags:
        - browser-api
      operationId: listTasks
      security:
        - session: []
      description: >-
        Every background activity this build registers, in the order a status
        page reads best.
      responses:
        "200":
          description: The registered tasks.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TaskList'
        "401":
          $ref: '#/components/responses/Unauthorized'
        "403":
          $ref: '#/components/responses/Forbidden'
  /v1/tasks/{name}/schedule:
    put:
      tags:
        - browser-api
      operationId: setTaskSchedule
      security:
        - session: []
          browserOrigin: []
      description: >-
        Sets whether the schedule may start this task. It governs unattended
        runs only: a task switched off still runs when an operator asks for it.
      parameters:
        - $ref: '#/components/parameters/TaskName'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TaskScheduleUpdate'
      responses:
        "200":
          description: The registered tasks, as they now stand.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TaskList'
        "400":
          $ref: '#/components/responses/InvalidRequest'
        "401":
          $ref: '#/components/responses/Unauthorized'
        "403":
          $ref: '#/components/responses/Forbidden'
        "404":
          $ref: '#/components/responses/NotFound'
        "503":
          $ref: '#/components/responses/Unavailable'
  /v1/tasks/{name}/run:
    post:
      tags:
        - browser-api
      operationId: runTask
      security:
        - session: []
          browserOrigin: []
      description: >-
        Starts one attempt of a task and reports whether it was accepted. An
        accepted attempt continues independently of this request.
      parameters:
        - $ref: '#/components/parameters/TaskName'
      responses:
        "202":
          $ref: '#/components/responses/Accepted'
        "401":
          $ref: '#/components/responses/Unauthorized'
        "403":
          $ref: '#/components/responses/Forbidden'
        "404":
          $ref: '#/components/responses/NotFound'
        "409":
          $ref: '#/components/responses/TaskInProgress'
  /v1/tasks/{name}/run/{argument}:
    post:
      tags:
        - browser-api
      operationId: runTaskArgument
      security:
        - session: []
          browserOrigin: []
      description: >-
        Starts one attempt of a task over one argument, such as a single target
        slot. Otherwise identical to running it with none. The `404` is about
        the task name; an unconfigured argument is the task's own to interpret
        and is accepted.
      parameters:
        - $ref: '#/components/parameters/TaskName'
        - $ref: '#/components/parameters/TaskArgument'
      responses:
        "202":
          $ref: '#/components/responses/Accepted'
        "401":
          $ref: '#/components/responses/Unauthorized'
        "403":
          $ref: '#/components/responses/Forbidden'
        "404":
          $ref: '#/components/responses/NotFound'
        "409":
          $ref: '#/components/responses/TaskInProgress'
  /v1/tasks/runs:
    get:
      tags:
        - browser-api
      operationId: getTaskRuns
      description: >-
        What the background activities have been doing, newest first: one page
        of the recorded history, over every task or over one named task.
      parameters:
        - name: task
          in: query
          description: >-
            Narrows the page to one registered task. A name this build does not
            register is refused rather than answered with an empty page.
          schema:
            type: string
        - name: after
          in: query
          schema:
            type: string
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 100
            default: 20
      responses:
        "200":
          description: A newest-first cursor page.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TaskRunPage'
        "400":
          $ref: '#/components/responses/InvalidRequest'
        "401":
          $ref: '#/components/responses/Unauthorized'
        "403":
          $ref: '#/components/responses/Forbidden'
        "503":
          $ref: '#/components/responses/Unavailable'
  /v1/sync/runs:
    get:
      tags:
        - browser-api
      operationId: getSyncRuns
      parameters:
        - name: after
          in: query
          schema:
            type: string
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 100
            default: 20
      responses:
        "200":
          description: A newest-first cursor page.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SyncRunPage'
        "400":
          $ref: '#/components/responses/InvalidRequest'
        "401":
          $ref: '#/components/responses/Unauthorized'
        "403":
          $ref: '#/components/responses/Forbidden'
        "503":
          $ref: '#/components/responses/Unavailable'
  /v1/activities:
    get:
      tags:
        - browser-api
      operationId: getActivities
      description: >-
        The recorded activities of one target, newest first. A caller reads
        only the target they own; an admin may name any. The window is
        half-open on the start time with no maximum span, and no more than
        5000 activities are served in one response.
      parameters:
        - name: from
          in: query
          description: >-
            Inclusive start of the window. Defaults to the account's first
            recorded activity.
          schema:
            type: string
            format: date-time
        - name: to
          in: query
          description: Exclusive end of the window. Defaults to now.
          schema:
            type: string
            format: date-time
        - name: target
          in: query
          description: >-
            The target to read. Omitted means the caller's own. A target the
            caller does not own is answered not found rather than forbidden,
            so the surface never confirms which targets exist.
          schema:
            type: string
      responses:
        "200":
          description: Recorded activities, newest first.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ActivityList'
        "400":
          $ref: '#/components/responses/InvalidRequest'
        "401":
          $ref: '#/components/responses/Unauthorized'
        "403":
          $ref: '#/components/responses/Forbidden'
        "404":
          $ref: '#/components/responses/NotFound'
        "503":
          $ref: '#/components/responses/Unavailable'
  /v1/activities/{activityId}/track:
    get:
      tags:
        - browser-api
      operationId: getActivityTrack
      description: >-
        One activity's recorded track, as a GeoJSON Feature. A caller reads
        only an activity of the target they own; an admin may name any target.
        An activity of another target, and one with fewer than two positioned
        samples, are both answered not found.
      parameters:
        - name: activityId
          in: path
          required: true
          schema:
            type: integer
            format: int64
        - name: target
          in: query
          description: >-
            The target to read. Omitted means the caller's own.
          schema:
            type: string
      responses:
        "200":
          description: The activity's recorded track.
          content:
            application/geo+json:
              schema:
                $ref: '#/components/schemas/ActivityTrack'
        "400":
          $ref: '#/components/responses/InvalidRequest'
        "401":
          $ref: '#/components/responses/Unauthorized'
        "403":
          $ref: '#/components/responses/Forbidden'
        "404":
          $ref: '#/components/responses/NotFound'
        "503":
          $ref: '#/components/responses/Unavailable'
  /v1/routes:
    get:
      tags:
        - browser-api
      operationId: getRoutes
      responses:
        "200":
          description: Stored route inventory without geometry.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RouteList'
        "401":
          $ref: '#/components/responses/Unauthorized'
        "403":
          $ref: '#/components/responses/Forbidden'
        "503":
          $ref: '#/components/responses/Unavailable'
  /v1/providers/{provider}/sourceRoutes/{sourceRouteId}/routes/{stageOrder}:
    get:
      tags:
        - browser-api
      operationId: getRoute
      parameters:
        - $ref: '#/components/parameters/Provider'
        - $ref: '#/components/parameters/SourceRouteId'
        - $ref: '#/components/parameters/StageOrder'
      responses:
        "200":
          description: Stored route metadata.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Route'
        "400":
          $ref: '#/components/responses/InvalidRequest'
        "401":
          $ref: '#/components/responses/Unauthorized'
        "403":
          $ref: '#/components/responses/Forbidden'
        "404":
          $ref: '#/components/responses/NotFound'
        "503":
          $ref: '#/components/responses/Unavailable'
  /v1/providers/{provider}/sourceRoutes/{sourceRouteId}/routes/{stageOrder}/geometry:
    get:
      tags:
        - browser-api
      operationId: getRouteGeometry
      parameters:
        - $ref: '#/components/parameters/Provider'
        - $ref: '#/components/parameters/SourceRouteId'
        - $ref: '#/components/parameters/StageOrder'
      responses:
        "200":
          description: Stored route geometry.
          content:
            application/geo+json:
              schema:
                $ref: '#/components/schemas/GeoJSONFeature'
        "400":
          $ref: '#/components/responses/InvalidRequest'
        "401":
          $ref: '#/components/responses/Unauthorized'
        "403":
          $ref: '#/components/responses/Forbidden'
        "404":
          $ref: '#/components/responses/NotFound'
        "503":
          $ref: '#/components/responses/Unavailable'
  /v1/providers/{provider}/sourceRoutes/{sourceRouteId}/routes/{stageOrder}/reprocess:
    post:
      tags:
        - browser-api
      operationId: reprocessRoute
      parameters:
        - $ref: '#/components/parameters/Provider'
        - $ref: '#/components/parameters/SourceRouteId'
        - $ref: '#/components/parameters/StageOrder'
      security:
        - session: []
          browserOrigin: []
      responses:
        "202":
          $ref: '#/components/responses/Accepted'
        "400":
          $ref: '#/components/responses/InvalidRequest'
        "401":
          $ref: '#/components/responses/Unauthorized'
        "403":
          $ref: '#/components/responses/Forbidden'
        "404":
          $ref: '#/components/responses/NotFound'
        "503":
          $ref: '#/components/responses/Unavailable'
  /v1/providers/{provider}/routes/{routeId}/stages/{stage}:
    get:
      operationId: redirectStageRoute
      parameters:
        - $ref: '#/components/parameters/Provider'
        - $ref: '#/components/parameters/LegacyRouteId'
        - $ref: '#/components/parameters/LegacyStage'
      responses:
        "308":
          $ref: '#/components/responses/LegacyRedirect'
        "400":
          $ref: '#/components/responses/InvalidRequest'
        "401":
          $ref: '#/components/responses/Unauthorized'
        "403":
          $ref: '#/components/responses/Forbidden'
        "404":
          $ref: '#/components/responses/NotFound'
  /v1/providers/{provider}/routes/{routeId}/stages/{stage}/geometry:
    get:
      operationId: redirectStageGeometry
      parameters:
        - $ref: '#/components/parameters/Provider'
        - $ref: '#/components/parameters/LegacyRouteId'
        - $ref: '#/components/parameters/LegacyStage'
      responses:
        "308":
          $ref: '#/components/responses/LegacyRedirect'
        "400":
          $ref: '#/components/responses/InvalidRequest'
        "401":
          $ref: '#/components/responses/Unauthorized'
        "403":
          $ref: '#/components/responses/Forbidden'
        "404":
          $ref: '#/components/responses/NotFound'
  /v1/providers/{provider}/routes/{routeId}/stages/{stage}/reprocess:
    post:
      operationId: redirectStageReprocess
      parameters:
        - $ref: '#/components/parameters/Provider'
        - $ref: '#/components/parameters/LegacyRouteId'
        - $ref: '#/components/parameters/LegacyStage'
      security:
        - session: []
          browserOrigin: []
      responses:
        "308":
          $ref: '#/components/responses/LegacyRedirect'
        "400":
          $ref: '#/components/responses/InvalidRequest'
        "401":
          $ref: '#/components/responses/Unauthorized'
        "403":
          $ref: '#/components/responses/Forbidden'
        "404":
          $ref: '#/components/responses/NotFound'
  /v1/routes/{routeId}/stages/{stage}:
    get:
      operationId: redirectLegacyRoute
      parameters:
        - $ref: '#/components/parameters/LegacyRouteId'
        - $ref: '#/components/parameters/LegacyStage'
      responses:
        "308":
          $ref: '#/components/responses/LegacyRedirect'
        "400":
          $ref: '#/components/responses/InvalidRequest'
        "401":
          $ref: '#/components/responses/Unauthorized'
        "403":
          $ref: '#/components/responses/Forbidden'
        "404":
          $ref: '#/components/responses/NotFound'
  /v1/routes/{routeId}/stages/{stage}/geometry:
    get:
      operationId: redirectLegacyGeometry
      parameters:
        - $ref: '#/components/parameters/LegacyRouteId'
        - $ref: '#/components/parameters/LegacyStage'
      responses:
        "308":
          $ref: '#/components/responses/LegacyRedirect'
        "400":
          $ref: '#/components/responses/InvalidRequest'
        "401":
          $ref: '#/components/responses/Unauthorized'
        "403":
          $ref: '#/components/responses/Forbidden'
        "404":
          $ref: '#/components/responses/NotFound'
  /v1/routes/{routeId}/stages/{stage}/reprocess:
    post:
      operationId: redirectLegacyReprocess
      parameters:
        - $ref: '#/components/parameters/LegacyRouteId'
        - $ref: '#/components/parameters/LegacyStage'
      security:
        - session: []
          browserOrigin: []
      responses:
        "308":
          $ref: '#/components/responses/LegacyRedirect'
        "400":
          $ref: '#/components/responses/InvalidRequest'
        "401":
          $ref: '#/components/responses/Unauthorized'
        "403":
          $ref: '#/components/responses/Forbidden'
        "404":
          $ref: '#/components/responses/NotFound'
  /v1/settings:
    get:
      tags:
        - browser-api
      operationId: getSettings
      responses:
        "200":
          description: The settings in force right now.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Settings'
        "401":
          $ref: '#/components/responses/Unauthorized'
        "403":
          $ref: '#/components/responses/Forbidden'
        "503":
          $ref: '#/components/responses/Unavailable'
  /v1/settings/wahoo:
    put:
      tags:
        - browser-api
      operationId: setWahooApplication
      security:
        - session: []
          browserOrigin: []
      description: >-
        Replaces the registered Wahoo application. The target slots are written
        separately: they are the targets this application writes to, not part
        of the application itself.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/WahooApplicationUpdate'
      responses:
        "200":
          $ref: '#/components/responses/SettingsStored'
        "400":
          $ref: '#/components/responses/InvalidRequest'
        "401":
          $ref: '#/components/responses/Unauthorized'
        "403":
          $ref: '#/components/responses/Forbidden'
        "503":
          $ref: '#/components/responses/Unavailable'
  /v1/settings/sources/{provider}:
    put:
      tags:
        - browser-api
      operationId: setSource
      security:
        - session: []
          browserOrigin: []
      description: >-
        Replaces one library and the account it is read with, leaving the other
        libraries exactly as they are. Turning a library off removes it from the
        list a run reads; the account it was read with stays stored, so turning
        it back on does not ask for the credentials again.
      parameters:
        - name: provider
          in: path
          required: true
          description: The library this edit is about.
          schema:
            type: string
            enum:
              - veloplanner
              - komoot
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SourceUpdate'
      responses:
        "200":
          $ref: '#/components/responses/SettingsStored'
        "400":
          $ref: '#/components/responses/InvalidRequest'
        "401":
          $ref: '#/components/responses/Unauthorized'
        "403":
          $ref: '#/components/responses/Forbidden'
        "503":
          $ref: '#/components/responses/Unavailable'
  /v1/settings/notifications:
    put:
      tags:
        - browser-api
      operationId: setNotifications
      security:
        - session: []
          browserOrigin: []
      description: Replaces the notification settings and the Pushover credentials.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/NotificationsUpdate'
      responses:
        "200":
          $ref: '#/components/responses/SettingsStored'
        "400":
          $ref: '#/components/responses/InvalidRequest'
        "401":
          $ref: '#/components/responses/Unauthorized'
        "403":
          $ref: '#/components/responses/Forbidden'
        "503":
          $ref: '#/components/responses/Unavailable'
  /v1/settings/alerts:
    put:
      tags:
        - browser-api
      operationId: setAlerts
      security:
        - session: []
          browserOrigin: []
      description: >-
        Records which alerts are delivered. Deciding is what creates a record,
        so an alert left out keeps whatever it had.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AlertsUpdate'
      responses:
        "200":
          $ref: '#/components/responses/SettingsStored'
        "400":
          $ref: '#/components/responses/InvalidRequest'
        "401":
          $ref: '#/components/responses/Unauthorized'
        "403":
          $ref: '#/components/responses/Forbidden'
        "503":
          $ref: '#/components/responses/Unavailable'
  /v1/settings/basemaps:
    put:
      tags:
        - browser-api
      operationId: setBasemaps
      security:
        - session: []
          browserOrigin: []
      description: >-
        Replaces the basemap list whole, in the order it is offered. The first
        is what a browser that has never chosen one loads.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/BasemapsUpdate'
      responses:
        "200":
          $ref: '#/components/responses/SettingsStored'
        "400":
          $ref: '#/components/responses/InvalidRequest'
        "401":
          $ref: '#/components/responses/Unauthorized'
        "403":
          $ref: '#/components/responses/Forbidden'
        "503":
          $ref: '#/components/responses/Unavailable'
  /v1/settings/surface:
    put:
      tags:
        - browser-api
      operationId: setSurface
      security:
        - session: []
          browserOrigin: []
      description: Replaces the surface index's regions and rebuild cadence, and starts a rebuild.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SurfaceSettings'
      responses:
        "200":
          $ref: '#/components/responses/SettingsStored'
        "400":
          $ref: '#/components/responses/InvalidRequest'
        "401":
          $ref: '#/components/responses/Unauthorized'
        "403":
          $ref: '#/components/responses/Forbidden'
        "503":
          $ref: '#/components/responses/Unavailable'
  /v1/settings/timezone:
    put:
      tags:
        - browser-api
      operationId: setTimezone
      security:
        - session: []
          browserOrigin: []
      description: >-
        Replaces the zone this service reads local time in. A zone it cannot
        load is refused, because a schedule with no local time has no time to
        run at.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TimezoneUpdate'
      responses:
        "200":
          $ref: '#/components/responses/SettingsStored'
        "400":
          $ref: '#/components/responses/InvalidRequest'
        "401":
          $ref: '#/components/responses/Unauthorized'
        "403":
          $ref: '#/components/responses/Forbidden'
        "503":
          $ref: '#/components/responses/Unavailable'
  /v1/settings/sync:
    put:
      tags:
        - browser-api
      operationId: setSync
      security:
        - session: []
          browserOrigin: []
      description: >-
        Replaces the settings a run reads when it starts, including the gate
        that lets an empty library delete a target's routes.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SyncSettings'
      responses:
        "200":
          $ref: '#/components/responses/SettingsStored'
        "400":
          $ref: '#/components/responses/InvalidRequest'
        "401":
          $ref: '#/components/responses/Unauthorized'
        "403":
          $ref: '#/components/responses/Forbidden'
        "503":
          $ref: '#/components/responses/Unavailable'
  /v1/webui/config:
    get:
      tags:
        - browser-api
      operationId: getWebUIConfig
      responses:
        "200":
          description: Browser-only runtime configuration.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WebUIConfig'
        "401":
          $ref: '#/components/responses/Unauthorized'
        "403":
          $ref: '#/components/responses/Forbidden'
  /v1/weather:
    get:
      tags:
        - browser-api
      operationId: getWeather
      parameters:
        - name: point
          in: query
          required: true
          style: form
          explode: true
          allowReserved: true
          schema:
            type: array
            minItems: 1
            maxItems: 48
            items:
              type: string
              pattern: ^-?[0-9]+(\.[0-9]+)?,-?[0-9]+(\.[0-9]+)?,.+$
      responses:
        "200":
          description: Forecast samples in request order.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WeatherForecast'
        "400":
          $ref: '#/components/responses/InvalidRequest'
        "401":
          $ref: '#/components/responses/Unauthorized'
        "403":
          $ref: '#/components/responses/Forbidden'
        "502":
          $ref: '#/components/responses/ProviderUnavailable'
  /v1/weather-grid/latest:
    get:
      tags:
        - browser-api
      operationId: getWeatherGridLatest
      description: >-
        Relays the model's own capture manifest — its reference time and the
        valid times it currently publishes — from Open-Meteo's spatial data
        files, so the browser's reader never reaches a third party directly.
      responses:
        "200":
          description: The manifest, exactly as the provider publishes it.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WeatherGridManifest'
        "401":
          $ref: '#/components/responses/Unauthorized'
        "403":
          $ref: '#/components/responses/Forbidden'
        "502":
          $ref: '#/components/responses/ProviderUnavailable'
  /v1/weather-grid/object:
    get:
      tags:
        - browser-api
      operationId: getWeatherGridObject
      description: >-
        Relays one .om file's bytes for the run named by referenceTime and
        the hour named by validTime. The browser's own reader decides which
        byte ranges it needs and asks for them with its own Range header,
        which this operation forwards unchanged.
      parameters:
        - $ref: '#/components/parameters/WeatherGridReferenceTime'
        - $ref: '#/components/parameters/WeatherGridValidTime'
      responses:
        "200":
          description: The whole object.
          content:
            application/octet-stream: {}
        "206":
          description: The requested byte range.
          content:
            application/octet-stream: {}
        "400":
          $ref: '#/components/responses/InvalidRequest'
        "401":
          $ref: '#/components/responses/Unauthorized'
        "403":
          $ref: '#/components/responses/Forbidden'
        "502":
          $ref: '#/components/responses/ProviderUnavailable'
    head:
      # No tag, so Orval never generates a client for it — the browser's own
      # reader (@openmeteo/file-reader) issues this directly, the same way it
      # issues its own Range reads, never through a generated binding.
      operationId: headWeatherGridObject
      description: >-
        The same relay as GET, without a body — the reader's own probe for
        Accept-Ranges and Content-Length before it starts asking for ranges.
      parameters:
        - $ref: '#/components/parameters/WeatherGridReferenceTime'
        - $ref: '#/components/parameters/WeatherGridValidTime'
      responses:
        "200":
          description: The object exists; headers only, no body.
        "400":
          $ref: '#/components/responses/InvalidRequest'
        "401":
          $ref: '#/components/responses/Unauthorized'
        "403":
          $ref: '#/components/responses/Forbidden'
        "502":
          $ref: '#/components/responses/ProviderUnavailable'
  /oauth/wahoo/start:
    get:
      tags:
        - oauth
      operationId: startOwnOAuth
      description: >-
        Starts the flow for the caller's own target, creating it on first use.
        The bare path exists because the browser is never told its own
        subject, so it cannot name a target the way an admin starting
        another's does.
      responses:
        "302":
          $ref: '#/components/responses/OAuthRedirect'
        "400":
          $ref: '#/components/responses/AuthorizationFailed'
        "401":
          $ref: '#/components/responses/Unauthorized'
        "403":
          $ref: '#/components/responses/Forbidden'
        "503":
          $ref: '#/components/responses/Unavailable'
  /oauth/wahoo/start/{target}:
    get:
      tags:
        - oauth
      operationId: startOAuth
      parameters:
        - $ref: '#/components/parameters/Target'
      responses:
        "302":
          $ref: '#/components/responses/OAuthRedirect'
        "400":
          $ref: '#/components/responses/AuthorizationFailed'
        "401":
          $ref: '#/components/responses/Unauthorized'
        "403":
          $ref: '#/components/responses/Forbidden'
        "503":
          $ref: '#/components/responses/Unavailable'
  /oauth/wahoo/callback:
    get:
      tags:
        - oauth
      operationId: completeOAuth
      parameters:
        - name: state
          in: query
          schema:
            type: string
        - name: code
          in: query
          schema:
            type: string
      responses:
        "303":
          $ref: '#/components/responses/BrowserRedirect'
        "400":
          $ref: '#/components/responses/AuthorizationFailed'
        "401":
          $ref: '#/components/responses/Unauthorized'
        "403":
          $ref: '#/components/responses/Forbidden'
components:
  securitySchemes:
    session:
      type: apiKey
      in: cookie
      name: __Host-domestique_session
      description: Browser session cookie issued by the sign-in flow.
    browserOrigin:
      type: apiKey
      in: header
      name: Origin
      description: Exact browser origin configured for this service. Required by every operation that starts a run or writes state, and modelled as a security scheme rather than a parameter because a request without it is refused rather than misread.
  parameters:
    WeatherGridReferenceTime:
      name: referenceTime
      in: query
      required: true
      schema:
        type: string
        format: date-time
    WeatherGridValidTime:
      name: validTime
      in: query
      required: true
      schema:
        type: string
        format: date-time
    TaskName:
      name: name
      in: path
      required: true
      description: The task's registered name, such as sync or surface:index.
      schema:
        type: string
        minLength: 1
    TaskArgument:
      name: argument
      in: path
      required: true
      description: What the attempt is over, such as a target slot name.
      schema:
        type: string
        minLength: 1
    Target:
      name: target
      in: path
      required: true
      schema:
        type: string
        minLength: 1
    Provider:
      name: provider
      in: path
      required: true
      schema:
        type: string
        minLength: 1
    SourceRouteId:
      name: sourceRouteId
      in: path
      required: true
      schema:
        type: integer
        format: int64
        minimum: 1
    StageOrder:
      name: stageOrder
      in: path
      required: true
      schema:
        type: integer
        minimum: 1
    LegacyRouteId:
      name: routeId
      in: path
      required: true
      description: The source route identifier, under the name a superseded address gave it.
      schema:
        type: integer
        format: int64
        minimum: 1
    LegacyStage:
      name: stage
      in: path
      required: true
      description: The stage order, under the name a superseded address gave it.
      schema:
        type: integer
        minimum: 1
  responses:
    SettingsStored:
      description: >-
        Every setting now in force, not only the section this request replaced,
        so one answer refills the whole page.
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Settings'
    Accepted:
      description: Work was accepted to run asynchronously.
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Accepted'
    InvalidRequest:
      description: Request is malformed.
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Error'
    Unauthorized:
      description: Missing or invalid browser session.
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Error'
    Forbidden:
      description: Session identity or origin is not permitted.
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Error'
    NotFound:
      description: Resource was not found.
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Error'
    Unavailable:
      description: Local state is unavailable.
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Error'
    ProviderUnavailable:
      description: Provider request failed without exposing provider detail.
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Error'
    TaskInProgress:
      description: >-
        The attempt was refused. This exact work is already happening, or
        something it needs is held by another run. Neither is a fault.
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Error'
    AuthorizationFailed:
      description: OAuth request failed safely.
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Error'
    OAuthRedirect:
      description: Redirect to Wahoo authorization.
      headers:
        Location:
          required: true
          schema:
            type: string
            format: uri
    BrowserRedirect:
      description: Redirect back to browser UI.
      headers:
        Location:
          required: true
          schema:
            type: string
    LegacyRedirect:
      description: Permanent method-preserving redirect.
      headers:
        Location:
          required: true
          schema:
            type: string
  schemas:
    Error:
      type: object
      additionalProperties: false
      required:
        - error
      properties:
        error:
          type: object
          additionalProperties: false
          required:
            - code
            - message
          properties:
            code:
              type: string
            message:
              type: string
    Health:
      type: object
      additionalProperties: false
      required:
        - status
      properties:
        status:
          type: string
          const: ok
    Readiness:
      type: object
      additionalProperties: false
      required:
        - status
      properties:
        status:
          type: string
          const: ready
    Unready:
      type: object
      additionalProperties: false
      required:
        - status
        - reason
      properties:
        status:
          type: string
          const: unready
        reason:
          type: string
          enum:
            - state_unreadable
            - state_incomplete
    Accepted:
      type: object
      additionalProperties: false
      required:
        - status
      properties:
        status:
          type: string
          const: accepted
    TargetRoutes:
      type: object
      additionalProperties: false
      required:
        - current
        - pending
      properties:
        current:
          type: integer
          minimum: 0
        pending:
          type: integer
          minimum: 0
    TargetRun:
      type: object
      additionalProperties: false
      required:
        - completedAt
        - result
      properties:
        completedAt:
          type: string
          format: date-time
        result:
          type: string
        failure:
          type: string
    TargetStatus:
      type: object
      additionalProperties: false
      required:
        - id
        - authorisation
        - convergence
        - routes
      properties:
        id:
          type: string
        owner:
          type: string
          description: >-
            The subject that owns this target. Present only for an admin
            caller: a non-admin's own target is already known to be theirs,
            and never sees another's here at all.
        own:
          type: boolean
          description: >-
            True when this target belongs to the calling subject. Present
            only for an admin caller, whose list also carries other riders'
            targets; a non-admin's list holds only their own.
        authorisation:
          type: string
        convergence:
          type: string
          enum:
            - current
            - lagging
            - failed
            - unauthorized
        routes:
          $ref: '#/components/schemas/TargetRoutes'
        lastRun:
          $ref: '#/components/schemas/TargetRun'
    RouteValidation:
      type: object
      additionalProperties: false
      required:
        - biasPercent
        - maePercent
        - p90Percent
        - evaluatedRides
      properties:
        biasPercent:
          type: number
          format: double
        maePercent:
          type: number
          format: double
        p90Percent:
          type: number
          format: double
        evaluatedRides:
          type: integer
          minimum: 0
    Route:
      type: object
      additionalProperties: false
      required:
        - provider
        - sourceRouteId
        - stageOrder
        - title
        - sourceRouteName
        - routeName
        - sourceRevision
        - contentHash
        - distanceMetres
        - ascentMetres
        - descentMetres
        - maxGradientPercent
        - pointCount
      properties:
        provider:
          type: string
        sourceRouteId:
          type: integer
          format: int64
        stageOrder:
          type: integer
        title:
          type: string
        sourceRouteName:
          type: string
        routeName:
          type: string
        sourceRevision:
          type: string
        contentHash:
          type: string
        distanceMetres:
          type: number
          format: double
        ascentMetres:
          type: number
          format: double
        descentMetres:
          type: number
          format: double
        maxGradientPercent:
          type: number
          format: double
        pointCount:
          type: integer
        movingSeconds:
          type: number
          format: double
        validation:
          $ref: '#/components/schemas/RouteValidation'
    Activity:
      type: object
      additionalProperties: false
      required:
        - id
        - startedAt
        - distanceMetres
        - movingSeconds
        - elapsedSeconds
        - ascentMetres
        - typeId
        - locationId
      properties:
        id:
          type: integer
          format: int64
        startedAt:
          type: string
          format: date-time
        distanceMetres:
          type: number
          format: double
        movingSeconds:
          type: number
          format: double
        elapsedSeconds:
          type: number
          format: double
        ascentMetres:
          type: number
          format: double
        typeId:
          type: integer
        locationId:
          type: integer
    ActivityList:
      type: object
      additionalProperties: false
      required:
        - activities
      properties:
        activities:
          type: array
          items:
            $ref: '#/components/schemas/Activity'
    RouteList:
      type: object
      additionalProperties: false
      required:
        - routes
      properties:
        routes:
          type: array
          items:
            $ref: '#/components/schemas/Route'
    SyncPhaseRun:
      type: object
      additionalProperties: false
      required:
        - lastCompletedAt
        - lastResult
        - sourceRoutes
        - created
        - updated
        - deleted
      properties:
        lastCompletedAt:
          type: string
          format: date-time
        lastResult:
          type: string
        lastFailure:
          type: string
        sourceRoutes:
          type: integer
        created:
          type: integer
        updated:
          type: integer
        deleted:
          type: integer
    SyncPhases:
      type: object
      additionalProperties: false
      properties:
        source:
          $ref: '#/components/schemas/SyncPhaseRun'
        targets:
          $ref: '#/components/schemas/SyncPhaseRun'
    SyncActive:
      type: object
      additionalProperties: false
      required:
        - targets
        - routes
      properties:
        phase:
          type: string
          enum:
            - source
            - targets
        startsAt:
          type: string
          format: date-time
        targets:
          type: integer
          minimum: 0
        routes:
          $ref: '#/components/schemas/TargetRoutes'
    SurfaceCoverage:
      type: object
      additionalProperties: false
      required:
        - classified
        - total
        - incomplete
        - enrichmentFailures
      properties:
        generation:
          type: string
        builtAt:
          type: string
          format: date-time
        classified:
          type: integer
        total:
          type: integer
        incomplete:
          type: integer
        enrichmentFailures:
          description: >-
            How many stages currently have a surface or duration enrichment
            pass recorded against them as unable to finish.
          type: integer
    WahooRateLimit:
      type: object
      additionalProperties: false
      required:
        - remaining
        - observedAt
      properties:
        remaining:
          type: integer
        resetsAt:
          type: string
          format: date-time
        observedAt:
          type: string
          format: date-time
    TrustedInventory:
      type: object
      additionalProperties: false
      required:
        - ageSeconds
        - maxAgeSeconds
        - fresh
      properties:
        lastSuccessAt:
          type: string
          format: date-time
        ageSeconds:
          type: integer
        maxAgeSeconds:
          type: integer
        fresh:
          type: boolean
    SyncStatus:
      type: object
      additionalProperties: false
      required:
        - state
        - phases
        - surface
        - sourceRoutes
        - created
        - updated
        - deleted
      properties:
        state:
          type: string
        active:
          $ref: '#/components/schemas/SyncActive'
        lastResult:
          type: string
        lastCompletedAt:
          type: string
          format: date-time
        sourceRoutes:
          type: integer
        created:
          type: integer
        updated:
          type: integer
        deleted:
          type: integer
        phases:
          $ref: '#/components/schemas/SyncPhases'
        surface:
          $ref: '#/components/schemas/SurfaceCoverage'
        wahooRateLimit:
          $ref: '#/components/schemas/WahooRateLimit'
        trustedInventory:
          $ref: '#/components/schemas/TrustedInventory'
    Build:
      type: object
      additionalProperties: false
      required:
        - revision
      properties:
        revision:
          type: string
        imageDigest:
          type: string
    Status:
      type: object
      additionalProperties: false
      required:
        - ready
        - converged
        - targets
        - sync
      properties:
        ready:
          type: boolean
        converged:
          type: boolean
        build:
          $ref: '#/components/schemas/Build'
        targets:
          type: array
          items:
            $ref: '#/components/schemas/TargetStatus'
        sync:
          $ref: '#/components/schemas/SyncStatus'
    SyncRun:
      type: object
      additionalProperties: false
      required:
        - reference
        - phase
        - completedAt
        - result
        - sourceRoutes
        - created
        - updated
        - deleted
      properties:
        reference:
          type: string
        phase:
          type: string
          enum:
            - source
            - targets
        completedAt:
          type: string
          format: date-time
        result:
          type: string
        failure:
          type: string
        sourceRoutes:
          type: integer
        created:
          type: integer
        updated:
          type: integer
        deleted:
          type: integer
    SyncRunPage:
      type: object
      additionalProperties: false
      required:
        - runs
      properties:
        runs:
          type: array
          items:
            $ref: '#/components/schemas/SyncRun'
        next:
          type: string
    Settings:
      type: object
      additionalProperties: false
      description: >-
        The settings held in the state database rather than in the
        configuration file, which take effect on the next run or the next
        request rather than on the next restart.
      required:
        - timezone
        - sync
        - notifications
        - basemaps
        - surface
        - wahoo
        - sources
        - rideModel
        - alerts
        - secretsSet
        - missing
      properties:
        timezone:
          type: string
          description: >-
            The IANA zone this service reads local time in: what a scheduled
            time of day means, and what hour a forecast describes.
        sync:
          $ref: '#/components/schemas/SyncSettings'
        notifications:
          $ref: '#/components/schemas/NotificationSettings'
        basemaps:
          type: array
          minItems: 1
          items:
            $ref: '#/components/schemas/BrowserBasemap'
        surface:
          $ref: '#/components/schemas/SurfaceSettings'
        wahoo:
          $ref: '#/components/schemas/WahooSettings'
        sources:
          type: array
          items:
            $ref: '#/components/schemas/SourceSettings'
          description: >-
            The libraries a run reads, in the order it reads them. An empty list
            is a service nobody has configured yet, not an error.
        rideModel:
          $ref: '#/components/schemas/RideModelStatus'
        alerts:
          type: array
          items:
            $ref: '#/components/schemas/AlertSetting'
          description: >-
            Every alert this service can raise, and whether it is delivered. An
            alert nobody has ruled on is delivered: a fault nobody has heard of
            is the one worth hearing about.
        secretsSet:
          type: object
          additionalProperties:
            type: boolean
          description: >-
            Whether each credential is stored. This is the whole of what any
            observable surface is told about one: never the value, only that
            there is one to replace.
        missing:
          type: array
          items:
            type: string
          description: >-
            The settings still to be entered, in the order the page offers
            them. Everything a run needs is here, and so are the Pushover
            credentials while notifications are on, which no run needs but every
            notification does. Empty is a configured service.
    WahooApplicationUpdate:
      type: object
      additionalProperties: false
      description: >-
        The registered application, without the slots it writes to. Those are
        replaced on their own.
      required:
        - apiBaseUrl
        - oauthBaseUrl
        - clientId
      properties:
        apiBaseUrl:
          type: string
          description: The origin route reads and writes are sent to.
        oauthBaseUrl:
          type: string
          description: The origin an authorization and a token refresh are sent to.
        clientId:
          type: string
          description: The registered application's public identifier.
        clientSecret:
          type: string
          description: >-
            The application's secret, sent only when it was typed into the form.
            Left out it keeps the stored value, which is how a page that was
            never told the secret can save the rest of the application; sent
            empty it removes the credential.
    SourceUpdate:
      type: object
      additionalProperties: false
      description: One library, and the account it is read with.
      required:
        - read
        - baseUrl
      properties:
        read:
          type: boolean
          description: >-
            Whether a run reads this library at all. Off takes it out of the
            list without forgetting the account it was read with.
        baseUrl:
          type: string
          description: >-
            The source's own web application, which is both the origin the
            service reads and the one a route is linked back to.
        email:
          type: string
          description: >-
            The account's email address, sent only when it was typed. The rules
            the application secret follows apply here too.
        password:
          type: string
          description: The account's password, on the same terms as the email address.
    AlertSetting:
      type: object
      additionalProperties: false
      required:
        - task
        - alert
        - enabled
        - decided
      properties:
        task:
          type: string
          description: The background activity the alert is about.
        alert:
          type: string
          description: >-
            The stable reason the alert names. It never carries provider text, a
            route name, or an upstream identifier.
        enabled:
          type: boolean
          description: Whether this alert is delivered.
        decided:
          type: boolean
          description: >-
            Whether anybody has ruled on it. An alert nobody has ruled on is
            delivered, which is not the same as one somebody switched on.
    AlertsUpdate:
      type: object
      additionalProperties: false
      required:
        - alerts
      properties:
        alerts:
          type: array
          items:
            $ref: '#/components/schemas/AlertDecision'
          description: >-
            What the operator has decided. Deciding is what creates a record, so
            an alert left out of this list keeps whatever it had.
    AlertDecision:
      type: object
      additionalProperties: false
      required:
        - task
        - alert
        - enabled
      properties:
        task:
          type: string
        alert:
          type: string
        enabled:
          type: boolean
    NotificationsUpdate:
      type: object
      additionalProperties: false
      required:
        - enabled
        - pushoverBaseUrl
      properties:
        enabled:
          type: boolean
          description: >-
            The switch for the whole channel. Off, nothing is sent at all: a
            failure, a blocked run, a recovery and a task that has stopped
            succeeding are held back as surely as a routine success is. Which
            alerts are sent while it is on is a decision per task and per
            reason, held in the alert matrix.
        pushoverBaseUrl:
          type: string
          format: uri
          description: >-
            The origin the application token and user key are sent to.
        applicationToken:
          type: string
          description: >-
            The Pushover application's token, sent only when it was typed. The
            rules the application secret follows apply here too.
        userKey:
          type: string
          description: The recipient key, on the same terms as the application token.
    BasemapsUpdate:
      type: object
      additionalProperties: false
      required:
        - basemaps
      properties:
        basemaps:
          type: array
          minItems: 1
          items:
            $ref: '#/components/schemas/BrowserBasemap'
    TaskScheduleUpdate:
      type: object
      additionalProperties: false
      required:
        - enabled
      properties:
        enabled:
          type: boolean
          description: >-
            Whether the schedule may start this task. It governs unattended runs
            only.
    TaskList:
      type: object
      additionalProperties: false
      required:
        - tasks
      properties:
        tasks:
          type: array
          items:
            $ref: '#/components/schemas/Task'
    Task:
      type: object
      additionalProperties: false
      required:
        - name
        - scheduled
        - enabled
        - running
      properties:
        name:
          type: string
          description: What a trigger asks for.
        scheduled:
          type: boolean
          description: Whether the task has a schedule at all.
        enabled:
          type: boolean
          description: >-
            Whether the schedule may start it. A task nobody has ruled on runs.
            It governs unattended runs only.
        running:
          type: integer
          description: How many attempts of this task are in flight.
        intervalSeconds:
          type: integer
          description: >-
            The gap between runs for a task on a fixed schedule. Absent for a
            task with no schedule, or a calendar one, whose gap changes with
            the wall clock.
        nextRunAt:
          type: string
          format: date-time
          description: >-
            When the next scheduled run is due. Absent for a task nothing
            schedules, and while the task's own scheduled run is under way. A
            run somebody asked for does not disturb the schedule, so this stays
            put during one.
    TaskRun:
      type: object
      additionalProperties: false
      description: >-
        One recorded attempt. It carries the aggregate record and nothing about
        what the attempt touched.
      required:
        - task
        - startedAt
        - finishedAt
        - outcome
        - reference
      properties:
        task:
          type: string
        argument:
          type: string
          description: >-
            What the attempt was over, such as a target slot. Absent for a task
            that covers only one thing.
        trigger:
          type: string
          description: >-
            What started the attempt: `schedule`, `manual`, or `chain`. Absent
            for an attempt recorded before this was written down.
        startedAt:
          type: string
          format: date-time
        finishedAt:
          type: string
          format: date-time
        outcome:
          type: string
        detail:
          type: string
          description: >-
            A stable, safe-to-display reason for the outcome. Never provider
            text, a route name, or an upstream identifier.
        reference:
          type: string
          description: The name an alert about this attempt carries.
    TaskRunPage:
      type: object
      additionalProperties: false
      required:
        - runs
      properties:
        runs:
          type: array
          items:
            $ref: '#/components/schemas/TaskRun'
        next:
          type: string
    TimezoneUpdate:
      type: object
      additionalProperties: false
      required:
        - timezone
      properties:
        timezone:
          type: string
          description: >-
            An IANA zone name this service can load, such as Europe/Berlin.
    SyncSettings:
      type: object
      additionalProperties: false
      required:
        - allowEmptySourceDeletion
        - staleAfterSeconds
        - initialDelaySeconds
      properties:
        allowEmptySourceDeletion:
          type: boolean
          description: >-
            Whether a source that reports an empty library may delete the last
            routes a target holds. It stays on until it is turned off again.
        staleAfterSeconds:
          type: integer
          minimum: 1
          description: >-
            How long the trusted source inventory may go without a successful
            refresh before it is reported and notified as stale.
        initialDelaySeconds:
          type: integer
          minimum: 1
          description: >-
            How long after start the first run is attempted. It is read by the
            start it delays, so an edit reaches the next restart rather than the
            next run.
    NotificationSettings:
      type: object
      additionalProperties: false
      required:
        - enabled
        - pushoverBaseUrl
      properties:
        enabled:
          type: boolean
          description: >-
            The switch for the whole channel. Off, nothing is sent at all: a
            failure, a blocked run, a recovery and a task that has stopped
            succeeding are held back as surely as a routine success is. Which
            alerts are sent while it is on is a decision per task and per
            reason, held in the alert matrix.
        pushoverBaseUrl:
          type: string
          format: uri
          description: >-
            The origin the application token and user key are sent to.
    WahooSettings:
      type: object
      additionalProperties: false
      required:
        - apiBaseUrl
        - oauthBaseUrl
        - clientId
      properties:
        apiBaseUrl:
          type: string
          description: >-
            The origin route reads and writes are sent to. Empty until the
            application is registered, which is a service that runs nothing
            rather than one that is broken.
        oauthBaseUrl:
          type: string
          description: The origin an authorization and a token refresh are sent to.
        clientId:
          type: string
          description: >-
            The registered application's public identifier. Its secret is not
            here: it is written through secrets and read back only as set.
    SourceSettings:
      type: object
      additionalProperties: false
      required:
        - provider
        - baseUrl
      properties:
        provider:
          type: string
          enum:
            - veloplanner
            - komoot
        baseUrl:
          type: string
          description: >-
            The source's own web application, which is both the origin the
            service reads and the one a route is linked back to.
    RideModelStatus:
      type: object
      additionalProperties: false
      description: >-
        The coefficient pair predicted moving time is priced with. Read-only:
        it is replaced by a calibration, not by an operator.
      required:
        - secondsPerKm
        - secondsPerAscentM
        - source
        - evaluatedRides
      properties:
        secondsPerKm:
          type: number
          description: Seconds added per kilometre ridden.
        secondsPerAscentM:
          type: number
          description: Seconds added per metre climbed.
        source:
          type: string
          enum:
            - default
            - calibrated
          description: >-
            Whether these are the built-in values every deployment starts with
            or the ones a calibration fitted.
        calibrationCutoff:
          type: string
          format: date
          description: The last day of riding the fit that produced them read.
        evaluatedRides:
          type: integer
          description: >-
            How many unseen rides the fit's error was measured over. Zero is
            not measured, which is what the built-in pair always reports.
    SurfaceSettings:
      type: object
      additionalProperties: false
      required:
        - regions
        - rebuildIntervalSeconds
      properties:
        regions:
          type: array
          items:
            type: string
          description: >-
            The Geofabrik region paths the surface index is built from. An empty
            list switches classification off.
        rebuildIntervalSeconds:
          type: integer
          minimum: 1
          description: >-
            How often the index is rebuilt. Required whether or not a region is
            named: the rebuild schedule runs either way, and with no region it
            simply builds nothing.
    BrowserBasemap:
      type: object
      additionalProperties: false
      required:
        - name
        - styleUrl
      properties:
        name:
          type: string
          minLength: 1
        styleUrl:
          type: string
          format: uri
        styleUrlDark:
          type: string
          format: uri
        darkCartography:
          type: boolean
    WebUIConfig:
      type: object
      additionalProperties: false
      required:
        - basemaps
        - timezone
        - identity
      properties:
        basemaps:
          type: array
          minItems: 1
          items:
            $ref: '#/components/schemas/BrowserBasemap'
        sourceBaseUrls:
          $ref: '#/components/schemas/SourceBaseUrls'
        timezone:
          type: string
          description: The IANA zone the service reads local time in.
        identity:
          $ref: '#/components/schemas/BrowserIdentity'
    BrowserIdentity:
      type: object
      additionalProperties: false
      description: >-
        Who the gate let through. It names the reader rather than identifying
        them: it is how a session can be seen to be the one intended.
      required:
        - display
        - admin
      properties:
        display:
          type: string
        admin:
          type: boolean
          description: >-
            Whether this subject holds cross-subject rights. Asserted by the
            same Auth0 Action that separately decides whether this subject
            may sign in at all — that decision is what admits the session in
            the first place, and holds regardless of this field's value.
    SourceBaseUrls:
      type: object
      additionalProperties: false
      properties:
        veloplanner:
          type: string
          format: uri
        komoot:
          type: string
          format: uri
    SurfaceRange:
      type: object
      additionalProperties: false
      required:
        - kind
        - startIndex
        - endIndex
      properties:
        kind:
          type: string
          enum:
            - asphalt
            - paving
            - compacted
            - gravel
            - ground
            - unknown
        startIndex:
          type: integer
          minimum: 0
        endIndex:
          type: integer
          minimum: 0
    GeoJSONLineString:
      type: object
      additionalProperties: false
      required:
        - type
        - coordinates
      properties:
        type:
          type: string
          const: LineString
        coordinates:
          type: array
          minItems: 2
          items:
            type: array
            minItems: 2
            maxItems: 3
            items:
              type: number
              format: double
    ActivityTrackLineString:
      type: object
      additionalProperties: false
      required:
        - type
        - coordinates
      properties:
        type:
          type: string
          const: LineString
        coordinates:
          type: array
          minItems: 2
          items:
            type: array
            minItems: 2
            maxItems: 2
            items:
              type: number
              format: double
    ActivityTrack:
      type: object
      additionalProperties: false
      required:
        - type
        - bbox
        - geometry
        - properties
      properties:
        type:
          type: string
          const: Feature
        bbox:
          type: array
          minItems: 4
          maxItems: 4
          items:
            type: number
            format: double
        geometry:
          $ref: '#/components/schemas/ActivityTrackLineString'
        properties:
          $ref: '#/components/schemas/ActivityTrackProperties'
    ActivityTrackProperties:
      type: object
      additionalProperties: false
      properties:
        altitudeMetres:
          type: array
          items:
            type: number
            format: double
    GeoJSONProperties:
      type: object
      additionalProperties: false
      required:
        - provider
        - sourceRouteId
        - stageOrder
        - title
        - sourceRouteName
        - routeName
        - distanceMetres
        - ascentMetres
        - descentMetres
        - maxGradientPercent
        - pointCount
      properties:
        provider:
          type: string
        sourceRouteId:
          type: integer
          format: int64
        stageOrder:
          type: integer
        title:
          type: string
        sourceRouteName:
          type: string
        routeName:
          type: string
        distanceMetres:
          type: number
          format: double
        ascentMetres:
          type: number
          format: double
        descentMetres:
          type: number
          format: double
        maxGradientPercent:
          type: number
          format: double
        pointCount:
          type: integer
        surface:
          type: object
          additionalProperties: false
          required:
            - ranges
            - matchedMetres
          properties:
            ranges:
              type: array
              items:
                $ref: '#/components/schemas/SurfaceRange'
            matchedMetres:
              type: number
              format: double
        cumulativeSeconds:
          type: array
          items:
            type: number
            format: double
    GeoJSONFeature:
      type: object
      additionalProperties: false
      required:
        - type
        - bbox
        - geometry
        - properties
      properties:
        type:
          type: string
          const: Feature
        bbox:
          type: array
          minItems: 4
          maxItems: 4
          items:
            type: number
            format: double
        geometry:
          $ref: '#/components/schemas/GeoJSONLineString'
        properties:
          $ref: '#/components/schemas/GeoJSONProperties'
    WeatherPoint:
      type: object
      additionalProperties: false
      required:
        - time
        - temperatureCelsius
        - apparentTemperatureCelsius
        - precipitationMillimetres
        - precipitationProbabilityPercent
        - windSpeedKmh
        - windDirectionDegrees
        - weatherCode
        - cloudCoverPercent
      properties:
        time:
          type: string
          format: date-time
        temperatureCelsius:
          type: number
          format: double
        apparentTemperatureCelsius:
          type: number
          format: double
        precipitationMillimetres:
          type: number
          format: double
        precipitationProbabilityPercent:
          type: number
          format: double
        windSpeedKmh:
          type: number
          format: double
        windDirectionDegrees:
          type: number
          format: double
        weatherCode:
          type: integer
        cloudCoverPercent:
          type: number
          format: double
    WeatherForecast:
      type: object
      additionalProperties: false
      required:
        - points
      properties:
        points:
          type: array
          items:
            $ref: '#/components/schemas/WeatherPoint'
    WeatherGridManifest:
      description: >-
        The provider's own capture manifest, relayed rather than reshaped:
        additional fields it publishes are passed through rather than
        rejected.
      type: object
      additionalProperties: true
      required:
        - reference_time
        - valid_times
      properties:
        reference_time:
          type: string
        valid_times:
          type: array
          items:
            type: string
